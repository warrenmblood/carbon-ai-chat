import { ArgTypes, Markdown, Meta } from '@storybook/addon-docs/blocks';

import { cdnJs, cdnCss } from "../../../globals/internal/storybook-cdn";
import * as ReasoningStepsToggleStories from "./reasoning-steps-toggle.stories";

<Meta of={ReasoningStepsToggleStories} parameters={{ previewTabs: { "storybook/docs/panel": { title: "Overview" } } }} />

# Reasoning steps toggle

[Source code](https://github.com/carbon-design-system/carbon-ai-chat/tree/main/packages/ai-chat-components/src/components/reasoning-steps) &nbsp;|&nbsp; [Feedback](https://github.com/carbon-design-system/carbon-ai-chat/issues)


## Table of Contents
- [Overview](#overview)
- [Component API](#component-api)
- [JS (via import)](#js-via-import)
- [JS (via CDN)](#js-via-cdn)
- [Feedback](#feedback)


## Overview

`cds-aichat-reasoning-steps-toggle` is a dedicated control for expanding or collapsing a `cds-aichat-reasoning-steps` wrapper. Use it when the host application owns the open state instead of relying on custom markup.

### Getting started

```html
<script type="module">
  const toggle = document.querySelector("cds-aichat-reasoning-steps-toggle");
  const wrapper = document.querySelector("cds-aichat-reasoning-steps");

  toggle.addEventListener("reasoning-steps-toggle", (event) => {
    wrapper.open = event.detail?.open;
  });
</script>

<cds-aichat-reasoning-steps-toggle
  panel-id="reasoning-toggle-panel"
  open-label-text="Hide reasoning steps"
  closed-label-text="Show reasoning steps"
></cds-aichat-reasoning-steps-toggle>

<cds-aichat-reasoning-steps id="reasoning-toggle-panel" open>
  <cds-aichat-reasoning-step title="Collect context" open>
    Combined prompt, chat history, and system instructions.
  </cds-aichat-reasoning-step>
  <cds-aichat-reasoning-step title="Draft plan">
    Outlined the answer and validated it against requirements.
  </cds-aichat-reasoning-step>
</cds-aichat-reasoning-steps>
```

## Component API

<ArgTypes of="cds-aichat-reasoning-steps-toggle" />

## JS (via import)

```javascript
import "@carbon/ai-chat-components/es/components/reasoning-steps/index.js";
```

<Markdown>{`${cdnJs(["reasoning-steps"])}`}</Markdown>
<Markdown>{`${cdnCss()}`}</Markdown>

## Feedback

Open an issue at https://github.com/carbon-design-system/carbon-ai-chat/issues to provide feedback or request changes.
